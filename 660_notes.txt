1. For every pair of points compute a vector by adding these points together, do not double count (x2 + x1, y2 + y1)
2. Add these to an array
3. Check for when x and y are equal among two points, then us k choose 2 where k is the number of coordinates equal to each other.
